import { createTheme } from "@mui/material/styles";

const Theme = createTheme({
  palette: {
    primary: {
      // #The Black color
      main: "#000000",
    },
    secondary: {
      // The grey color
      main: "grey",
    },
  },
});

export default Theme;
